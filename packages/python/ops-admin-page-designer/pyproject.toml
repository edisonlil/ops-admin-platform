[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ops-admin-page-designer"
version = "0.1.0"
description = "Page designer bounded context for ops-admin-platform."
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "ops-admin-system",
    "ops-admin-identity-access",
]

[project.entry-points."ops_admin.routers"]
page_designer = "page_designer.entrypoints:router"

[project.entry-points."ops_admin.init_tasks"]
page_designer = "page_designer.entrypoints:init_tasks"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
page_designer = ["infrastructure/persistence/*.sql"]

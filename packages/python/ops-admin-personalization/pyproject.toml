[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ops-admin-personalization"
version = "0.1.0"
description = "Personalization bounded context for ops-admin-platform."
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "ops-admin-identity-access",
    "ops-admin-system",
]

[project.entry-points."ops_admin.routers"]
personalization = "personalization.entrypoints:router"

[project.entry-points."ops_admin.init_tasks"]
personalization = "personalization.entrypoints:init_tasks"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
personalization = ["infrastructure/persistence/*.sql"]

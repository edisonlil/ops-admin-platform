[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ops-admin-ai-service-api"
version = "0.1.0"
description = "Stable AI service contracts for ops-admin-platform bounded contexts."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
